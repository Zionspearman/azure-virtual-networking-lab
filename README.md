# azure-virtual-networking-lab
Implement Virtual Networking in Azure. Built CoreServicesVnet and ManufacturingVnet (ARM template), configured subnets, secured traffic with NSG + ASG rules, and set up Public + Private Azure DNS zones with record validation using nslookup.
